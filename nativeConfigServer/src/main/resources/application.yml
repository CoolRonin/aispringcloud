server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
#    读取本地配置
  profiles:
    active: native
#    本地配置文件存放的路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared