server:
  port: 8020
spring:
  application:
    name: consumer

#    注册中心的访问地址
#      是否将当前服务的 IP 注册到 Eureka Server。
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true


