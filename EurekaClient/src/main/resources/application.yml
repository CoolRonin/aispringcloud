server:
  port: 8011
#  port: 8010
spring:
  application:
#    微服务的名字
    name: provider

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
